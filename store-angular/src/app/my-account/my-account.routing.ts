import { ModuleWithProviders } from "@angular/core";
import { Routes, RouterModule } from "@angular/router";

const myAccountRoutes: Routes = [];

export const myAccountRouting: ModuleWithProviders = RouterModule.forRoot(
  myAccountRoutes
);
